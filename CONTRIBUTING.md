# 为 Echo Town 做贡献

感谢你帮助构筑一个会自行演进的虚拟小镇。所有代码和世界内容变更都通过 Pull Request 提交；仓库初始化完成后，不直接修改默认分支。

## 开始之前

1. Fork `spalagu/echo-town` 并从最新 `main` 创建主题分支。
2. 配置清晰、可追溯的 Git 提交身份。
3. 运行 `npm run check`，确认治理和许可证入口完整。
4. 每个提交使用 `git commit -s` 添加 Developer Certificate of Origin 签署行，声明你有权贡献相关内容。

## 世界内容贡献

世界内容 Pull Request 可以增加地点、职业、资源、初态、情境种子、规则原语、开放谜团、美术、音乐或本地化。Pull Request 必须说明：

- 新增了哪些初始条件或行动可能性；
- 哪些事实对哪些角色可观察；
- 角色能够采取哪些行动；
- 资源如何产生、消耗、过期或回收；
- 正常、故障和边界状态分别是什么；
- 文本和素材的作者、来源、许可证与修改记录。

以下内容不会被接受：

- 主线、支线、剧情阶段或章节目标；
- 指定必须参与的角色；
- 必达转折、预期结果、标准答案或预定结局；
- 无来源事实、远程脚本、HTML 或可执行内容；
- 把舆论热度直接写成世界真值；
- 把事后故事摘要作为角色目标或世界控制输入。

## 代码贡献

- AI 只能生成结构化 Intent，不得直接修改权威世界状态。
- 确定性 World Core 的状态变化必须可重放并产生一致的状态摘要。
- 私人记忆、提示词和主观关系默认不得出站。
- 新功能必须包含正常、失败和边界用例；改坏核心断言时检查必须变红。
- 不得引入依赖未公开私有系统的构建步骤。

## Pull Request 说明

Pull Request 应保持单一目的，并包含：变更动机、用户可见影响、验证命令、失败与回退方式、许可证或素材归属。维护者会审查规则一致性、非剧本化边界、安全性和可复验性，不会要求故事按某个作者预期发展。
